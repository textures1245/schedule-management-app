import { HrCenterDirective } from './hr-center.directive';

describe('HrCenterDirective', () => {
  it('should create an instance', () => {
    const directive = new HrCenterDirective();
    expect(directive).toBeTruthy();
  });
});
